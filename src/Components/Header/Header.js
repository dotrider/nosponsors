import React from 'react';
import './Header.css';

const Footer = () => {

    return(
        <header>
            <nav className='navBar'>
                 <a>Home</a>
                <a>About</a>
            </nav>
        </header>
    )
}

export default Footer